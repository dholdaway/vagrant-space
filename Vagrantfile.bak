ENV['VAGRANT_DEFAULT_PROVIDER'] = 'rackspace'
Vagrant.configure("2") do |config|
  config.vm.box = "dummy"

  config.vm.hostname = "live01"
  config.vm.provider :rackspace do |rs|
  config.vm.provision "shell", path:  "bootstrap.sh"
    rs.username = "darrenholdaway"
    rs.api_key  = "cf7da018a049872a5699e944dd0335e3"
    rs.flavor   = /512MB/
    rs.image    = "Ubuntu 12.10 (Quantal Quetzal)"
    rs.public_key_path = "/Users/darrenholdaway/.ssh/id_rsa.pub"
    config.vm.provision "shell", path:  "bootstrap.sh"
end
end
